# Tangle Business MVP — Order Service

A simple order processing service demonstrating Tangle's capabilities:
- Struct definitions with typed fields
- Method definitions with `this` sugar
- Error handling with `@error`, `?`, and `Err()`
- Immutable struct updates with `with { }`

## Usage

```bash
tangle run examples/mvp/order-service.tangle.md
```
