# MathDataDemo

Demonstrates Math, Random, and Sort operations.

#### main

```@tangle
let x = Math.abs(-5)
let r = Random.int_range(1, 100)
let sorted = Sort.asc([3, 1, 2])
let biggest = Sort.max(sorted)
return biggest
```
