# String

### StringUtils

#### StringUtils -> 长度 (length)
@export
* `s`: input string (String)

```@tangle
return s.length
```

#### StringUtils -> 拼接 (concat)
@export
* `a`: first (String)
* `b`: second (String)

```@tangle
return a + b
```
